---
type: daily
tags: [diario, hermes]
title: Diário 2026-08-26-1811-20260826_124
description: Daily note gerada pelo wiki_review
timestamp: 2026-08-26T18:11:14-03:00
status: draft
---


## Identidade visual aprovada mora na RAIZ — apagar o perdedor inteiro — 18:11
Quando o Giovani aprova UMA identidade (cor, fonte, layout), ela vai em `:root` do globals.css + layout raiz + shell único, sem `data-theme`, sem variantes, sem scaffold do tema perdedor sobrevivendo (hook, views duplicadas, componentes órfãos). Depois rodar `tsc --noEmit` até zerar órfãos de tipos. Meio-aposentar código morto quebrou a sessão: `task-row.tsx` órfão só apareceu no erro quando contrato mudou.

## Eyebrow é proibido em QUALQUER OM projeto — caçar os disfarçados — 18:11
Regra universal dele, reforçada 3× na sessão: nenhum overline uppercase acima de título ou número ("OPERAÇÃO · SEMANA CORRENTE", "HORAS NO PERÍODO · TOTAL"). Ele achou um que tinha sobrevivido disfarçado de label de dado acima do KPI gigante. Também proibido: legendas descritivas sob números e subtítulo redundante sob título. Aplicar a PageHeader/components compartilhados, não só à página em foco.

## Escolha do usuário num comparativo vence recomendação anterior — sempre — 18:11
Ele escolheu Newsreader no comparativo; eu migrei pra Fraunces depois por conta própria num pedido ambíguo → rejeição dura ("eu nao autorizei e nao quero a Fraunces. eu pedi NEWSREADER"). Quando ele escolhe entre opções apresentadas, registrar a escolha e nunca trocar sem autorização explícita — nem recomendando "melhor", nem interpretando pedido ambíguo como mudança.

## Snapshot estático: webfont precisa de link + CSS vars no head gerado — 18:11
Dois bugs em camada: `next/font` não injeta no HTML cru DO renderToStaticMarkup E as vars CSS (`--font-*`) ficam na classe do `<body>`, que o snapshot não renderiza. Fix completo no snapshot.mjs: `<link>` do Google Fonts + `<style>:root { --font-newsreader: 'Newsreader'; ... }</style>` no head gerado + `--virtual-time-budget=8000` no chromium. Sem isso o print cai no fallback e mente sobre a tipografia real.

## README de projeto = só técnica; wiki não recebe nada não-validado — 18:11
Ele detonou guia de print dentro de README ("não faz sentido readme de um SITE ter instruções de como tirar print"): README carrega o-que-é · modelo de domínio · stack · rotas · dev. E deletou página que criei na wiki com "regras universais" durante experimentação: projeto em testes/rascunho NÃO ganha página na wiki — documentar decisão antes de validação é prematuro e virou trabalho de limpeza.
