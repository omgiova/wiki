---
type: daily
tags: [diario, hermes]
title: Diário 2026-09-04-0857-20260903_143
description: Daily note gerada pelo wiki_review
timestamp: 2026-09-04T08:57:54-03:00
status: draft
---


## display.runtime_footer — só 4 campos existem; footer do usuário está encerrado — 08:40
Footer do gateway: `hermes config set display.runtime_footer.fields '["model","context_pct","latency","cwd"]'` (lista passa por parse YAML — não usar espaço entre elementos colados errado). Campos suportados: APENAS model, context_pct, latency (opt-in), cwd; o exemplo da doc oficial com "tool calls · $0.042" não está implementado; campo com dado indisponível é silenciosamente pulado (cwd não renderiza no gateway Telegram). Estado atual do usuário: [model, context_pct, latency, cwd], funcionando como ele quer — ele mandou "NÃO mexa em nada": não "corrigir" o cwd inativo nem reconfigurar o footer em sessões futuras.

## Guard pós-update 01/09 bloqueia scripts referenciados dentro do gateway — 08:50
O update de 01/09 (~3.7k commits) trouxe guard que lê o CONTEÚDO de qualquer script citado no comando terminal e escaneia o texto (`contains_gateway_lifecycle_command_or_referenced_script`). /root/scripts/ocg-usage (read-only) dispara falso positivo e é bloqueado; citar o caminho do script em QUALQUER comando também bloqueia (até comando de diagnóstico). Após um block: NÃO reintentar variantes do comando nem execute_code (guard proíbe; usuário nega). Caminho que funciona: executar fora do processo do gateway — pedir pro usuário rodar via SSH. Era o comportamento do guard antigo (pré-update) que o DeepSeek usava em 26/08 — não é mérito de modelo.

## Usuário desafia verificação e compara modelos quando script simples trava — 08:57
Duas correções fortes nesta sessão: (1) quando ele declara o comportamento real ("latency vem, cwd não aparece"), isso é ground truth — não deduzir do config nem oferecer mudança; insistir gerou "você está delirando... não mexa em nada!". (2) Quando algo simples trava (ocg-usage), ele questiona a competência e ameaça voltar pro modelo anterior ("devo voltar pro deepseek? ele conseguia com UM COMANDO") — responder com a causa real (mudou o guard do Hermes, não o modelo) em vez de insistir em tentativas. Perguntas tipo "tem certeza que adicionou SÓ o latency?" esperam resposta com o output exato do comando, não reafirmação.
