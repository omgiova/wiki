---
type: daily
tags: [diario, hermes]
title: Diário 2026-09-04-0955-20260830_195
description: Daily note gerada pelo wiki_review
timestamp: 2026-09-04T09:55:06-03:00
status: draft
---


## Guardian do gateway bloqueia scripts com literal `~/.hermes/...` — causa real e fix — 09:55
O guard do gateway lê o script referenciado e tokeniza o código como shell: o literal `~/.hermes/state.db` vira um "script referenciado" e a leitura dos ~205MB do state.db estoura o limite de 1MB (fail-closed) → bloqueio falso positivo. A causa NÃO é a palavra "reset" — a memória anterior está errada (trocar reset→renova não destrava; testado). Fix: montar caminhos com `os.path.join(os.path.expanduser("~"), ".hermes", ...)` — a v1 `/root/scripts/ocg-usage` fica bloqueada dentro do gateway; usar `/root/scripts/ocg-usage2`. Ao criar scripts que tocam state.db/.env, nunca escrever `~/.hermes/` literal no código/docstring — usar join, e validar com `contains_gateway_lifecycle_command_or_referenced_script` antes de rodar. Pendente: corrigir memória do "reset" e atualizar `tools/ocg-usage.md` com a v2.

## Sempre ler a página da wiki do tool antes de propor edição no script — 09:55
Correção do usuário ("vc leu o ocg-usage.md?") quando propus editar `/root/scripts/ocg-usage` sem ter consultado `tools/ocg-usage.md`. A wiki documenta implementação, limites e decisões já tomadas; propor mudança de script sem ler a página repete trabalho e gera correção. Aplicar: antes de qualquer proposta sobre tool/script, ler sua página em `wiki/tools/` (e seguir report-don't-correct do AGENTS.md).

## Timestamps do state.db são UTC; host em America/Sao_Paulo (-03) — 09:55
Para extrair "uso do dia desde 00:00", o ocg-usage2 só tem janelas relativas (15m..1w), não cutoff calendário. Query manual funcionou com `datetime.now().replace(hour=0, minute=0, ...)` → converte 00:00 local em timestamp UTC, que é a unidade dos campos `last_seen`/`first_seen` de `session_model_usage`. Aplicar: qualquer extração diária custom deve calcular o cutoff local assim; a flag `--since-today` na v2 ficou pendente (usuário não confirmou).

## Preferência: não alterar script que funciona — criar versão nova — 09:55
Usuário rejeitou editar a v1 mesmo com correção pronta ("era ótimo do jeito q estava, não vou desistir") e pediu uma v2. Aplicar: quando um script quebra por mudança de ambiente (guard, libs), preservar o original intacto e criar `/root/scripts/<nome>2` com o fix, mantendo comportamento idêntico — não mutar o arquivo que já provou funcionar.

## Causa real do bloqueio do ocg-usage: literal `~/.hermes/state.db` no código — NÃO "reset" — 09:50
O guard do gateway (cron/lifecycle_guard.py) lê scripts referenciados, tokeniza o código como shell e interpreta `~/.hermes/state.db` como "script referenciado" — tenta ler os 205MB do state.db e estoura o limite de 1MB (fail-closed). Memória antiga dizia "falso positivo: 'reset' no texto" — comprovadamente errado (trocar reset não destrava). Síntoma: `Blocked: command or referenced script cannot restart, stop, or uninstall the gateway...` ao rodar QUALQUER script que mencione caminhos do Hermes. Correção validada: montar caminhos sem o literal — `os.path.join(os.path.expanduser("~"), ".hermes", "state.db")` ou via `HERMES_HOME` (faz o guard não reconhecer path completo). Aplicar em scripts futuros; `/root/scripts/ocg-usage2` já tem a correção e roda dentro do gateway.

## Preferência de tabelas do Giovani: coluna de modelo contém SOMENTE modelos — 10:11
Corrigiu 3x na mesma sessão: subtotais/totais não podem ocupar a coluna de modelo, nem com rótulo ("─ SUBTOTAL"), nem com célula vazia. Estrutura que ele aprovou: grid dia×modelo só com linhas de modelo + tabela Σ POR DIA separada (sem coluna de modelo) + subtotal por modelo. Também rejeitou tabela única compacta ("perdi visualizações") — quer o cruzamento completo: sessões, reqs, input, output, cacheR, cacheW, reasoning, ponderado (in=1x, out=4x, cache=0.1x) e %plano estimado.

## ocg-usage2: correção validada mas NÃO oficializada — pendência de follow-up — 10:11
A v2 (`/root/scripts/ocg-usage2`) destrava o guard e ganhou formato grid completo + Σ POR DIA + %plano, mas a oficialização ficou em aberto: flag `--since <data>` ainda não implementada no script, listagem de sessões (seção ⑤) não confirmada, e falta documentar na wiki (tools/ocg-usage.md) a causa real do bloqueio + existência da v2. Retomar na próxima sessão se o Giovani quiser fechar.

## Estimativa de % do plano OCG por dia/modelo: método calibrado e validado — 10:11
A API do plano (opencode.ai/zen/go/v1/usage) só expõe % agregado (rolling/weekly/monthly) — sem quebra por dia/modelo. Método que funcionou: somar tokens do ciclo mensal no state.db (início do ciclo = monthly resetsAt ≈ 17/08), ponderar (in=1, out=4, cache=0.1), calibrar `monthly% / total ponderado` e escalar por célula. Resultado plausível (período 01–04/09 → 9.3% contra monthly 27%). $ em dólar é impossível de estimar: `estimated_cost_usd` no banco é sempre 0 (plano é assinatura, não pay-per-token).

## Guard do gateway bloqueia scripts com literal `~/.hermes/` — 10:02
O lifecycle guard lê o script referenciado, tokeniza o código como shell e interpreta `~/.hermes/state.db` como "script referenciado" — tenta abrir os ~200MB do state.db, estoura o limite de 1MB (fail-closed) e bloqueia. Testado: trocar palavra "reset" NÃO resolve; remover o literal `~/.hermes/` do **código** resolve (docstring não dispara). Fix: montar caminhos com `os.path.join(os.path.expanduser("~"), ".hermes", ...)` ou via env `HERMES_HOME`. Aplicar a todo script novo do /root/scripts que tocar state.db/.env; a v1 ocg-usage ficou inutilizável no gateway e a v2 (`/root/scripts/ocg-usage2`) é a corrigida.

## Tabelas markdown largas no Telegram não quebram — 10:13
Usuário corrigiu com ênfase forte: falácia "tabelas com 11 colunas ficam pesadas/quebram no Telegram" — NUNCA repetir. Tabelas largas (20-50 colunas) renderizam corretamente e são a visualização preferida dele. Aplicar: usar tabelas completas sem medo de largura; ele elogiou explicitamente o formato com todas as métricas.

## Não escrever em memória sem pedido explícito — 10:17
Usuário proibiu adicionar hipóteses/preferências à memória por conta própria: gasta tokens dele e a wiki existe para isso. Regra: conhecimento durável vai pra wiki; memória só com autorização. Não repetir autoadições de memória.

## state.db e API OCG não têm reqs+tokens por período — 10:28
Verificado empiricamente: `session_model_usage` é contador CUMULATIVO por sessão (filtro por janela só seleciona sessões via last_seen, números são do início da sessão — provado: 88 reqs em janela 3min vs 4 requests reais); `messages` tem granularidade por request (timestamp/tools/reasoning) mas `token_count` NULL em todas as 11k linhas; API `/zen/go/v1/usage` só dá % do plano (só 2 endpoints existem: usage e models, resto 404). Não tentar re-derivar isso do banco. Único caminho completo: capturar objeto `usage` das respostas da API no fluxo do Hermes e persistir por request — não implementado, adiado para follow-up.

## Gateway guard bloqueia scripts com literal de path grande — 10:40
Scripts executados dentro do gateway são lidos e tokenizados pelo guard (`cron/lifecycle_guard.py`): um literal como `~/.hermes/state.db` no código é interpretado como "script referenciado", e o guard tenta abrir o arquivo de ~200MB → estoura o limite de 1MB (fail-closed) e bloqueia o comando com falso positivo. Fix durável: montar caminhos via `HERMES_HOME`/`os.path.expanduser` + `os.path.join`, nunca com literal `~/.hermes` no arquivo. Verificar antes de executar: `contains_gateway_lifecycle_command_or_referenced_script('/path/script args', cwd='/root')` do módulo `cron.lifecycle_guard`.

## Usuário PROIBIU adições à memória do Hermes — 10:15
Regra explícita e com frustração: não usar a tool `memory` nem registrar hipóteses/preferências nela — "gasta meus tokens"; a wiki é o único mecanismo de conhecimento durável. Ao descobrir algo persistente, o destino é a wiki (com autorização), nunca a memória. Ignorar qualquer inclinação de "anotar na memória".

## Tabelas markdown no Telegram: sempre completas, nunca compactar — 10:50
Falácia corrigida com força ("jamais repetir"): tabelas com 11+ colunas renderizam certo no Telegram. Nunca "resumir" tabelas escondendo linhas/colunas na resposta (usuário percebe e rejeita). Nada de rótulo de subtotal/total na coluna de modelos — a coluna "modelo" só contém modelos; totais vão em tabela própria. Antibiótico durável: rodar o script com `--md` e colar o OUTPUT VERBATIM do script, sem reformatar.

## ocg-usage2: números são acumulados por sessão, não do período — 10:30
`session_model_usage` guarda contadores acumulados da sessão inteira; a janela só filtra quais sessões entram (via `last_seen`), então reqs/tokens exibidos NÃO correspondem ao período (ex: janela 3min mostrou 88 reqs, timeline real tinha 4). A tabela `messages` tem a timeline real por request (role='assistant' = 1 request) mas `token_count` é NULL — sem tokens por request em nenhuma fonte. API do OpenCode Go: só `/zen/go/v1/usage` e `/zen/go/v1/models` existem (sonda de 15 endpoints).

## Wiki: log.md é append-only — usar terminal `>>`, nunca write_file — 10:55
`write_file` SOBRESCREVE o arquivo inteiro; usado por engano no log.md (append-only de 964 linhas) destruiu o arquivo — restaurado via `git show HEAD:log.md`. Para appendar na wiki SEMPRE usar terminal com `printf '...' >> arquivo` ou verificar o arquivo inteiro antes. Lição reutilizável em toda edição de wiki com log.
