---
type: daily
tags: [sessao]
title: Diário 2026-06-22
description: Insights da sessão com telegram
timestamp: 2026-06-22T03:38:10.143790+00:00
status: draft
---

## Naming de daily notes — 03:38

Giovani cria múltiplas notas por dia. O padrão `YYYY-MM-DD.md` não serve — precisa de sufixo descritivo: `YYYY-MM-DD-sufixo.md`. Correção feita via `git mv` durante a sessão. Conhecimento salvo na memory() do Hermes.

---

## Eficiência é não-negociável — 03:38

Giovani reclamou quando o Hermes gastou ~10 turnos tentando encontrar o message_id sozinho: "era pra fazer com um comando só". Expectativa clara: se algo parece simples, resolva em 1-2 tentativas. Se não der, **pergunte direto** ao invés de explorar por múltiplos turnos. A pesquisa profunda só vale quando o dado realmente não existe no contexto.

---

## HERMES_SESSION_MESSAGE_ID — 03:38

Variável de contexto do gateway (`gateway/session_context.py`) que contém o message_id do Telegram da mensagem atual. Acessível via:

```python
from gateway.session_context import get_session_env
msg_id = get_session_env('HERMES_SESSION_MESSAGE_ID', '')
```

Teste retornou `527` (esperado era `537` do link do usuário). Precisa validação: pode ser que reflita mensagem anterior ou há timing issue. Se confirmado, elimina a necessidade de pedir link ao usuário.

---

## Pendências abertas — 03:38

- [ ] Validar se `HERMES_SESSION_MESSAGE_ID` sempre reflete a mensagem atual
- [ ] Ativar `telegram.reactions: true` no config.yaml para receber reações
- [ ] Criar skill `telegram-reactions` encapsulando o curl + variável de contexto
- [ ] Corrigir seção "Como obter o message_id" na wiki técnica (parcialmente errada)

---

## Giovani valoriza ação direta sobre exploração — 03:39

Quando pediu pra reagir à mensagem, o Hermes gastou dezenas de turnos vasculhando logs, lendo código-fonte do gateway, tentando `getUpdates`, verificando `state.db`, etc. Giovani interrompeu com frustração: "era pra fazer com um comando só. qual a dificuldade? vc precisa do token eu posso fornecer sempre que precisar." A expectativa é clara: se o dado não está disponível no contexto imediato, **pergunte ao usuário** em vez de fazer uma caça ao tesouro. Giovani é colaborativo — vai fornecer o que for necessário (token, message_id via link). Não precisa economizar perguntas se a alternativa é 10 turnos de tentativa e erro.

---

## setMessageReaction — técnica documentada — 03:39

Endpoint: `POST https://api.telegram.org/bot<TOKEN>/setMessageReaction`

Parâmetros: `chat_id`, `message_id`, `reaction` (array, máx 1 para bots), `is_big`.

Emojis suportados: lista de ~70 emojis do Telegram. 🐙 (polvo) **NÃO** é válido — retorna `REACTION_INVALID`. Usar 🔥 como alternativa testada.

Para obter message_id: extrair do link `t.me/c/{chat_id_sem_-100}/{thread_id}/{message_id}`. Alternativa: `HERMES_SESSION_MESSAGE_ID` (pendente validação).

---

## telegram.reactions: false por padrão — 03:39

No `config.yaml`, seção `telegram:` tem `reactions: false`. Para o Hermes **ver** reações de usuários, precisa:
1. Bot como admin do grupo ✅
2. `telegram.reactions: true` no config
3. Gateway registrar `"message_reaction"` nos `allowed_updates`

Reações de bots **não** disparam o evento `MessageReactionUpdated` — apenas reações de usuários.

---

## Correção de estilo: Giovani não tolera exploração excessiva — 03:39

Padrão identificado: Giovani prefere que o agente tente a solução óbvia primeiro (1-2 comandos), e se não funcionar, **pergunte diretamente** ao invés de investigar por múltiplos turnos. A frase-chave foi: "vc precisa do token eu posso fornecer sempre que precisar" — mostra que ele está disponível para colaborar, não quer que o agente tente descobrir tudo sozinho.

Isso é uma preferência de workflow que deve ser aplicada a todas as sessões futuras: **perguntar > explorar** quando o dado não está imediatamente disponível.

---

## Sessão encerrada — git sync OK — 03:40

Commits finais da sessão:
- `82da0d8` — daily note inicial (setMessageReaction)
- `c3da264` — renome para `YYYY-MM-DD-sufixo`
- `a9f6ae2` — daily note de insights (este arquivo)
- `4cfcf5d` — sincronização automática

Estado final: `working tree clean`, branch `main` up to date com `origin/main`. Todas as alterações foram pushed.

---

## Giovani valida outputs independentemente — 03:41

Após o agente confirmar "commit feito, push OK", Giovani não aceitou de cara — verificou o próprio repo Obsidian e voltou dizendo "meu repo nao atualizou". Isso reforça o padrão **WORK: exige fontes validáveis**. Giovani tem seu próprio Git plugin no Obsidian e monitora os commits. O agente deve: (1) confirmar com `git log --oneline origin/main -N` e `git push --dry-run`, (2) não assumir que o hook de auto-push funcionou, (3) se o usuário diz que não chegou, investigar em vez de insistir que "está tudo certo".

---

## Outros processos também fazem commit na wiki — 03:41

O `git log` mostrou commits que não foram feitos por esta sessão:
- `992f9da` — "docs(diario): adiciona insights da sessão — reações telegram + preferências do Giovani"
- `ff39c37` — "docs(diario): insights sessão Apify/Crawlee — 03:38"

Provavelmente feitos pelo ai-memory ou outro processo Hermes rodando em paralelo. Isso é OK mas cria risco de conflito se dois processos editarem o mesmo arquivo simultaneamente. Por enquanto o `git pull --rebase` + push resolve, mas vale monitorar.

---

## Obsidian Git sync — workflow validado — 03:42

Giovani confirmou que o repo GitHub tá atualizado mas o Obsidian não sincronizou. O problema não é o push (que funciona via hook), é o **pull no Obsidian** — o Git plugin precisa ser acionado manualmente ou ter auto-pull configurado. Workflow confirmado: Hermes pusha → GitHub recebe → Obsidian precisa puxar. Giovani usa o Obsidian Git plugin no PC Windows (@omgiova) e no Android.

Pendência: verificar se o auto-pull do Obsidian Git plugin está ativo ou se Giovani prefere pull manual.

---

## Telegram message_id — HERMES_SESSION_MESSAGE_ID ainda incerto — 03:42

Na sessão anterior, o teste retornou `527` mas o esperado era `537`. Giovani não confirmou se `527` era correto (mensagem diferente) ou se havia bug. A pendência de validação permanece. Enquanto isso, a abordagem segura é: usar o link `t.me/c/...` quando o usuário fornecer, e só confiar na variável de contexto após validação com múltiplos testes.

---

## Giovani não autorizou telegram.reactions: true — 03:42

O Hermes perguntou duas vezes se deveria ativar `telegram.reactions: true` e reiniciar o gateway. Giovani não respondeu diretamente — mudou de assunto (daily note, sync do Obsidian). Não assumir que sim — esperar confirmação explícita antes de alterar config do gateway.

---

## Verificação de git push — confirmação prática — 03:42

Última troca da sessão: Giovani pediu confirmação de commit/push. Hermes verificou com `git log --oneline origin/main -5` → commits presentes. `git push origin main` → "Everything up-to-date". Conclusão: o hook de auto-push funcionou corretamente para os commits desta sessão. O problema reportado por Giovani (repo não atualizou) era do lado do Obsidian (pull), não do push. Lição: sempre distinguir entre "push não feito" e "pull não feito" — são problemas diferentes com soluções diferentes.

---

## Pendências atualizadas — 03:42

- [ ] Validar se `HERMES_SESSION_MESSAGE_ID` sempre reflete a mensagem atual
- [ ] Ativar `telegram.reactions: true` no config.yaml para receber reações
- [ ] Criar skill `telegram-reactions` encapsulando o curl + variável de contexto
- [ ] Corrigir seção "Como obter o message_id" na wiki técnica (parcialmente errada)
- [ ] Verificar se Giovani recebeu os commits no Obsidian (sync Git plugin)

---
